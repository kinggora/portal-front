import { createStore } from "vuex";
import { authStore } from "@/store/modules/authStore";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    authStore,
  },
});
